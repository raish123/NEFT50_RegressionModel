#In this Module we are defining  the yaml filePath and Passing the filePath as window path
from pathlib import Path
import os,sys

CONFIG_FILEPATH = Path("config/config.yaml")
PARAM_FILEPATH =  Path("param.yaml")